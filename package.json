{
  "name": "lulu-trajet",
  "version": "5.1.0",
  "private": true,
  "description": "Apprendre le luxembourgeois en audio pendant les trajets.",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\"",
    "test:verbose": "node --test --test-reporter=spec \"tests/*.test.mjs\"",
    "contenu": "node scripts/build-content-ids.mjs cours.js cours.js cours.legacy-map.json"
  },
  "engines": { "node": ">=20" }
}
